// =============================================================================
// GITHUB PAGES DEMO CONFIG
// This file is committed with placeholder URLs for GitHub Pages display.
// The dashboard UI and 3D visualization will work fully in demo/offline mode.
// To connect to a live Firebase backend, replace the URLs below with your actual
// Firebase Realtime Database URLs and host config.js on your own server.
// =============================================================================
const CONFIG = {
    firebaseUrl: '',        // e.g. 'https://your-project.firebaseio.com/.json'
    controlUrl: '',         // e.g. 'https://your-project.firebaseio.com/control_commands.json'
    fetchInterval: 5000,
    location: {
        lat: 1.5437,        // Skudai, Johor — for real-time sun/moon calculation
        lon: 103.6366
    }
};
